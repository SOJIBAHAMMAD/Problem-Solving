#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main () {
   ios::sync_with_stdio(false);
   cin.tie(nullptr);

   int k;
   cin>>k;
   cout<< (1 << k)<<endl; // using left shift
   return 0;
}